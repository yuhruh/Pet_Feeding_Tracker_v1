<nav class="navbar navbar-expand-lg bg-body-tertiary mb-2">
  <div class="container-fluid">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/home">Home</a>
        </li>
        <% if logged_in? %>
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/users">Cat Feeders</a>
          </li>
      
          <li class="nav-item dropdown">
            <button class="btn btn-dark dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
              Tracker Records
            </button>
            <ul class="dropdown-menu">
              <li>
                <%= link_to "Adding Records", new_pet_tracker_path, class: "dropdown-item" %>
              </li>
              <li class="nav-item">
                <%= link_to "Show #{current_user.username.capitalize}'s Records", user_path(current_user), class: "dropdown-item" %>
              </li>
            </ul>
          </li>
          <li class="nav-item dropdown">
            <button class="btn btn-dark dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
              <%= "(Admin)" if current_user.admin? %> Profile <%= current_user.username.capitalize %>
            </button>
            <ul class="dropdown-menu">
              <li>
                <%= link_to "View your profile", user_path(current_user), class: "dropdown-item" %>
              </li>
              <li>
                <%= link_to "Edit your profile", edit_user_path(current_user), class: "dropdown-item" %>
              </li>
              <li>
                <%= link_to "Delete your profile", user_path(current_user), class: "dropdown-item text-danger", data: {"turbo-confirm": "Are you sure to delete your account? The whole data you created will gone...", "turbo-method": :delete } %>
              </li>
            </ul>
          </li>
          <li class="nav-item">
            <%= link_to "Log Out", logout_path, class: "nav-link", method: :delete %>
          </li>
        <% else %>
          <li class="nav-item">
            <a class="nav-link" href="/login">Log In</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" aria-current="page" href="/signup">Sign Up</a>
          </li>
        <% end%>
      </ul>
    </div>
  </div>
</nav>